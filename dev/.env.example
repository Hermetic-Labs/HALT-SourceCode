# HALT Build & Deploy — Environment Variables
# Copy to .env and fill in your values (never commit .env)
#
# Required for: python dev/build_and_deploy.py --deploy / --release
R2_ACCOUNT_ID=
R2_ACCESS_KEY=
R2_SECRET_KEY=
